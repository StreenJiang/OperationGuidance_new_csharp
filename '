using CustomLibrary.Panels;
using OperationGuidance_service.Apis;
using OperationGuidance_service.Configurations;
using OperationGuidance_service.Models;

namespace OperationGuidance_new.Views {
    public class MissionManagement: CustomContentPanel {
        private const int _tableColumns = 4;
        private const float _cellGap = 0.4F;
        private const float _cellHightRatio = 0.33F;
        private TableLayoutPanel _missionsTable;
        private List<ProductMission> _productMissions;
        private OperationGuidanceApis _apis;

        public MissionManagement() : base() {
            // Get apis
            OperationGuidanceApis? apis = DependencyInjector.Provider.GetService<OperationGuidanceApis>();
            
            //this.AutoScroll = true;
            //this.VScroll = false;
            _missionsTable = new();
            _missionsTable.Margin = new(0);
            _missionsTable.BackColor = Color.White;
            _missionsTable.Parent = this;
        }

        protected override void OnSizeChanged(EventArgs e) {
            base.OnSizeChanged(e);
            _missionsTable.Size = new(this.Width - this.Padding.Left * 2, this.Height - this.Padding.Top * 2);
        }

        private void FetchData() {

        }

    }
}
